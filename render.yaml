services:
  - type: web
    name: rasta-backend
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: EDGE_TTS_VOICE
        value: ur-PK-UzmaNeural
